// Global object

// console.log(global);
//global.setTimeout(() => {
//    console.log("I am Here!")
//}, 3000);

// mesma funcao que declarar sem o `global`

/* setTimeout(() => {
    console.log("I am Here!");
    clearInterval(int)
}, 3000);

const int = setInterval(() => {
    console.log('console log in the interval');
}, 1000);

console.log(__dirname);
console.log(__filename) */

// this does not exists on the global object, only on the Window on the browser
// console.log(document.querySelector)


